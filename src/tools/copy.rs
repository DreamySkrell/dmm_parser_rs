use dmmtools::dmm;

use crate::core::GridMap;
use crate::core::Tile;

// pub fn extract(src_map: &GridMap, coord: dmm::Coord2, size: dmm::Coord2) -> GridMap {
//     let mut dst_map = GridMap {
//         size: todo!(),
//         grid: todo!(),
//     };

//     for x in 0..size.x {
//         for y in 0..size.y {
//             if let Some(cell) = umm_src.grid.get(coord.x + x, coord.y + y) {
//                 *umm_dst.grid.get_mut(x, y).unwrap() = cell.clone();
//             }
//         }
//     }

//     dst_map
// }
